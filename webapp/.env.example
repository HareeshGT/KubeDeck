# KubeDeck Web is embedded in the desktop KubeDeck application.
# SSH credentials and the web login are NOT configured here.
#
# SSH access is inherited from the VM connection already established by
# KubeDeck, and web login credentials are stored in KubeDeck Settings.
#
# Optional web-server settings:
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8000
